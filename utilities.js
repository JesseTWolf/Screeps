module.exports = class nameGenerator {

    //Name Generator method.
    // static newName(role) {
    static newName() {
        // letnewName = role + Game.time;
        let newName = Game.time;
        return newName;
    }
}
